import { add } from './utils';

describe('some things', () => {

    it('should behave...', () => {
        expect(2 + 2).toBe(4);
    });
    it('can add DELETE ME', () => {
        expect(add(2,2)).toBe(4);
    });
}); 

// describe('calculate tip', () => {
//     it('should calculate tip', () => {
//         expect(calculateTip(10, 100).toBe(10));
//     });
// });