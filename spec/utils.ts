export const add = (a:number, b:number) => a + b;

// export function calculateTip(percent: number,amount: number) : number {    
//     //let factor:number = percent / 100;
//     //return factor * amount;
//     return 5 + 5;
// }

